# LandMap Israel 🏗️

Платформа визуализации земельных участков в Израиле для инвестиций в недвижимость.

## Стек технологий

- **Frontend:** React (Vite) + Tailwind CSS + Leaflet Maps
- **Backend:** Express.js + Supabase
- **AI Chat:** Claude (Anthropic)
- **Email:** Resend

## Быстрый старт

### 1. Настройка переменных окружения

```bash
cp .env.example .env
# Заполните значения в .env
```

### 2. Установка зависимостей

```bash
# Корневые зависимости (если есть)
npm install

# Сервер
cd server && npm install

# Клиент
cd ../client && npm install
```

### 3. Запуск

```bash
# Сервер (из папки server/)
npm start

# Клиент (из папки client/)
npm run dev
```

Сервер: `http://localhost:3001`  
Клиент: `http://localhost:5173`

## Структура проекта

```
├── client/                 # React фронтенд
│   ├── src/
│   │   ├── components/     # UI компоненты (MapArea, SidebarDetails, AIChat...)
│   │   ├── pages/          # Страницы (MapView, PlotDetail, Admin...)
│   │   ├── hooks/          # React хуки
│   │   ├── api/            # API клиент
│   │   └── utils/          # Утилиты и константы
│   └── index.html
├── server/                 # Express бэкенд
│   └── src/
│       ├── routes/         # API маршруты
│       ├── services/       # Бизнес-логика
│       ├── middleware/      # Auth, validation, rate limiting
│       ├── schemas/        # Zod-схемы валидации
│       └── config/         # Supabase, Claude, Email конфиги
└── supabase/               # Миграции и seed-данные
```

## API Endpoints

| Метод | Путь | Описание |
|-------|------|----------|
| GET | `/api/plots` | Список участков |
| GET | `/api/plots/:id` | Детали участка |
| POST | `/api/leads` | Создать лид |
| POST | `/api/chat` | AI чат |
| GET | `/api/pois` | Точки интереса |
| GET | `/api/health` | Статус сервера + БД |

## Переменные окружения

См. `.env.example` для полного списка.
