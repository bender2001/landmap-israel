{
  "name": "landmap-israel-fullstack",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "cd server && node --watch src/index.js",
    "dev:client": "cd client && npx vite",
    "build": "cd client && npm install && npx vite build",
    "start": "cd server && node src/index.js",
    "render-build": "npm install && cd server && npm install && cd ../client && npm install --include=dev && npx vite build",
    "seed": "cd server && node src/seed.js"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
